# projects/context

`projects/context/` は、この Context Repository 自身を管理するためのプロジェクト定義です。

## 役割

- このリポジトリ自身の `AGENTS.md` 正本を管理します。
- 将来的に context リポジトリ管理用の Skill をここに配置します。
- プロジェクト固有コンテキストの持ち方を定義する基準点になります。

## 正本

- `projects/context/AGENTS.md` を正本とします。
- 将来的には `context deploy context` によりルート `AGENTS.md` へ配布する想定です。
- 上記の deploy フローは現時点では未実装です。

## 将来配置する Skill 例

以下は例であり、現時点では未作成です。

- `skill-authoring`
- `readme-authoring`
- `agents-authoring`
